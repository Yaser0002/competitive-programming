#include <bits/stdc++.h>
#include<iomanip>
#include<numeric>
using namespace std;
bool isprime(int x){
  if(x==2)return true;
  else{
    for (int i = 2; i < x; i++)
    {
     if(x%i==0)return false;

    }
    
  }
  return true;
}
 void solve(){
 int xr,yr,xw,yw;
 cin>>xr>>yr>>xw>>yw;
 double resr=sqrt(xr*xr+yr*yr);
 double resw=sqrt(xw*xw+yw*yw);
 if(resr> resw)cout<<"Wil";
 else if(resr<resw)cout<<"Russo";
 else cout<<"Empate";
 }
int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  //int t;
//cin >> t;
//while(t--){
    solve();
//cout<<endl;}

}
